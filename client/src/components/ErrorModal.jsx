import { Dialog, DialogTitle } from "@mui/material";

function ErrorModal() {


  return (
    <Dialog open={true}>
      <DialogTitle>Error</DialogTitle>
    </Dialog>
  );
};

export default ErrorModal;